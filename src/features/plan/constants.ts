// Fixed row/label heights the drag-and-drop math depends on. The reorder
// animation computes offsets from these rather than measuring, so they must
// match the actual rendered heights in planStyles.
export const ITEM_HEIGHT = 56;
export const GROUP_LABEL_HEIGHT = 32;
